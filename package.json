{
  "name": "sql-agent-cli",
  "version": "1.0.0",
  "description": "Simple CLI to execute SQL on Supabase",
  "bin": {
    "sql-agent": "./bin/sql-agent",
    "ssql": "./bin/sql-agent"
  },
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:unit": "jest unit.test.ts",
    "test:e2e": "jest e2e.test.ts",
    "test:watch": "jest --watch",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "pg": "^8.11.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@types/pg": "^8.10.0",
    "@types/node": "^20.0.0",
    "@types/jest": "^29.5.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0"
  }
}